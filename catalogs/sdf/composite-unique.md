# Definisi Composite Unique (Composite Unique Definition)

Composite unique constraint dideklarasikan di array `uniques`:

```javascript
uniques: [
  ['stock_inbound_id', 'line_number']
]
```

Single column unique tetap menggunakan constraint `unique` di field-level.

---

**Lihat juga**: [`sdf/`](./) · [`catalogs/`](../) · [`README`](../../README.md)
