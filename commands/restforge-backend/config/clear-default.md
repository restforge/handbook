# `config clear-default`

> Menghapus default config untuk working directory saat ini.

## Pattern

```
npx restforge config clear-default
```

Command ini tidak menerima flag tambahan.

## Contoh

```bat
npx restforge config clear-default
```

---

**Lihat juga**: [`config/`](./) · [`commands/`](../) · [`README`](../../../README.md)
